# Local secrets and API keys. bootstrap.sh copies this to .env if needed.
# direnv loads .env automatically in bash and zsh once hooked.

# Free key: https://www.themoviedb.org/settings/api
# Used to snapshot US watch-provider catalogs (Netflix, Disney+, etc.).
TMDB_API_KEY=
